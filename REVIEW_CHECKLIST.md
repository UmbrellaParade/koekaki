# 点検チェックリスト

Codex が作ったものを引き渡す前、および第三者が点検するときに使います。
**「動くか」だけでなく「過去に踏んだ地雷を踏み直していないか」を見るのが目的です。**
各項目の背景は `HANDOFF.md` の第6章にあります。

---

## A. 壊していないか（毎回必ず）

- [ ] `npm test` が通る（46件。増えているのは可、減っているのは要説明）
- [ ] `npm run build` が通る
- [ ] `npm run build:desktop` と `npm run test:electron-desktop` が通る
- [ ] `npm run typecheck` がWeb版・Electron版とも通る
- [ ] `npm run dev` で `http://localhost:5173/koekaki/` が開く
- [ ] GitHub Actions のデプロイが成功している
- [ ] 公開URLが最新版になっている（設定画面のビルド日時で確認）

## B. 触ってはいけない設計判断

過去に不具合を出して、その対策として今の形になっている箇所です。
**変更されていたら、理由を必ず確認すること。**

- [ ] **スマホの単発モードが連続認識に戻されていない**（`prefersSingleShot`）
      → 戻すと「同じ文が羅列される」不具合が再発する
- [ ] **`dedupe.ts` の境界が緩められていない**（2文字の塊は対象外のまま）
      → 緩めると「いろいろ」が「いろ」に壊れる
- [ ] **Gemini へ送る音声が 16kHz WAV のまま**（WebM を直接送っていない）
- [ ] **内蔵音声認識と `MediaRecorder` を同時に起動していない**
- [ ] **認識途中の文字が既定で表示されていない**
- [ ] APIキー入力欄が `type="password"` に戻されていない
      （`autoCapitalize="none"` `autoCorrect="off"` も維持）
- [ ] 設定画面のビルド日時表示が残り、Web版では「最新版に更新」も表示される

## C. 失敗の扱い

- [ ] **どの経路でも、失敗したら必ず理由が画面に出る**（沈黙で終わらない）
- [ ] 使えない機能を黙って隠していない。押せない理由と対処が出る
- [ ] エラー文言に、次に何をすればいいかが書かれている

## D. 費用と依存

- [ ] **継続課金が発生する仕組みを足していない**（依頼主が最も嫌う点）
- [ ] 新しい npm 依存を足したなら、それが必要な理由が説明されている
- [ ] ビルドに追加のツールチェーン（Rust、VS Build Tools 等）を要求していない
- [ ] APIキーや個人情報がコミットされていない
      → `git log -p | grep -iE "sk-|AIza|api[-_]?key"` で確認

## E. Node 直実行テストの制約（`npm test` が壊れる原因）

- [ ] `src/lib` の**値**インポートに拡張子 `.ts` が付いている
- [ ] TypeScript のパラメータプロパティ構文（`constructor(private x: T)`）を使っていない
- [ ] `.ps1` ファイルが ASCII のみで書かれている（日本語コメントは化けて構文エラー）

## F. デスクトップ版を点検するとき

- [ ] 右 Alt が**トグル**で動く（押しっぱなしではない）
- [ ] **オーバーレイがフォーカスを奪わない**（`focusable: false`）
      → 奪うと貼り付け先が変わり、Ctrl+V が別の場所に飛ぶ
- [ ] 貼り付け後、元のクリップボード内容が復元される
- [ ] 右 Alt を押した瞬間に録音が始まる（体感の遅れがない）
- [ ] 既存の `src/lib` を再利用している（整形処理を二重に実装していない）
- [ ] アプリを終了してもフックのプロセスが残らない

## G. 引き渡しの説明

- [ ] **実機で確認できていないものが、確認済みとして書かれていない**
- [ ] 何をどう直したか、なぜそうしたかがコミットメッセージにある（日本語）
- [ ] Obsidian 側のバックアップが更新されている

---

## 点検を依頼するときの伝え方

点検者に次を伝えると、無駄な調査をせずに済みます。

```
点検してください。
- 対象: <ブランチ名 または コミット範囲。例: main の abc1234..def5678>
- やったこと: <1〜2行>
- 実機で確認できたこと / できていないこと:
- 気になっている箇所:
```

リポジトリは `C:\Users\myabe\Documents\koekaki` にあるので、
点検者は `git log` と `git diff` で差分を直接読めます。
