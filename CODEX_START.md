# Codex に最初に渡す文章

下の枠の中をそのままコピーして Codex に貼り付けてください。

---

こえかき（Koekaki）という音声入力アプリの開発を、Claude から引き継いでもらいます。

作業リポジトリ: `C:\Users\myabe\Documents\koekaki`

## 最初にやること

**`HANDOFF.md` を最初から最後まで読んでください。** 引き継ぎ書です。

特に次の2つの章は飛ばさないでください。実際に何度も失敗して得た手順が書いてあります。

- **第5章「不具合の直し方」** — 報告を受けたときに、どの順番で何を確認するか
- **第6章「踏んだ地雷」** — Gemini が WebM を受け付けない、Android の音声認識が
  結果を重複させる、スマホで API キーが入力できなくなる条件、など11件

読み終わったら、環境が動くことを確認してください。

```
npm install
npm test
npm run build
```

テストは56件あります（Web回帰26件 + ホットキープロトコル5件 +
貼り付けプロトコル10件 + デスクトップ契約15件）。
**1件でも落ちたら、先に進まずに報告してください。**

## 次にやること：デスクトップ常駐版

`HANDOFF.md` の第8章に、再現すべき仕様と調査済みの前提が書いてあります。

**やりたいこと**（Typeless と同じ挙動。公式ドキュメントで確認済み）:

- どのアプリを使っていても、**右 Alt** を押すと録音が始まる
- **もう一度右 Alt を押すと終了**（押しっぱなしではなくトグル）
- 整形済みのテキストが、**カーソル位置に直接挿入**される
- 録音中は画面にバーを出す。プレビューは無し

**制約**:

- この PC に Rust と Visual Studio Build Tools は無いので **Tauri は使えない。Electron で作る**
- Electron の `globalShortcut` では右 Alt 単独を登録できない。
  Windows の低レベルキーボードフックが必要で、**PowerShell の `Add-Type` を使えば
  追加インストール無しに C# を実行時コンパイルできる**
- 既存の `src/lib`（プロンプト・各社API・整形処理・辞書・設定）は**そのまま再利用できる**
- **追加の費用が発生する構成にはしないこと**（依頼主は月額課金を強く嫌います）

**現在は第4段階までコード実装・自動テスト済み**です。
次は第5段階のボイスバー仕上げです。

第4段階では、最初の右Altで前面トップレベルウィンドウの
HWND / PID / TID を保持し、整形完了時にも同じ対象である場合だけCtrl+Vを送ります。
対象変更、修飾キー押下、クリップボード競合、送信失敗時は自動貼り付けしません。

クリップボード復元は主要形式だけのbest effortです。
また、同じトップレベルウィンドウ内で別の入力欄へ移ったことは検出できません。
実マイク・実際の右Alt・実アプリへの貼り付けは、まだ依頼主による確認が必要です。

**進め方**: 一度に全部作らず、第8章末尾の5段階で刻んでください。
依頼主が途中経過を確認できるようにするためです。

**参考画像**: Typeless の録音中バーは受領済みです。
設定画面は依頼主側でも場所が分からず、追加で探してもらう必要はありません。

**追加されたスマホ要件**: こえかきは独自の文字キーボードを作らず、
手打ち時はユーザーが普段使う標準キーボードへ素早く戻します。
詳細は `HANDOFF.md` 第9章と `docs/mobile-input.md` を読んでください。

## 守ってほしいこと

1. 編集したらコミット＆プッシュ（GitHub Pages が公開版。押さないと依頼主が確認できない）
2. プッシュ後、Obsidian 側のバックアップも更新（`HANDOFF.md` 第10章にコマンドあり）
3. `npm test` を通してからコミット
4. コミットメッセージは日本語で、「なぜそうしたか」を書く
5. **実機確認できていないことは「できました」と言わない。**
   実マイク → AI整形 → 実アプリへのCtrl+V、各アプリでの入力先照合、
   クリップボード復元は依頼主のPCでしか最終確認できません

回答は日本語でお願いします。
