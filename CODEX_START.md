# Codex に最初に渡す文章

以下をそのままコピーして Codex に貼り付けてください。

---

こえかき（Koekaki）という音声入力アプリの開発を、Claude から引き継いでもらいます。

作業リポジトリ: `C:\Users\myabe\Documents\koekaki`

**まず `HANDOFF.md` を最初から最後まで読んでください。** 引き継ぎ書です。
このプロジェクトで既に踏んだ地雷（Gemini が WebM を受け付けない、Android の
音声認識が結果を重複させる、スマホでAPIキーが入力できなくなる条件など）が
すべて書いてあります。読まずに触ると同じ失敗を繰り返します。

読み終わったら、次の2つを順番にお願いします。

**1. 現状確認**

```
npm install
npm test
npm run build
```

が通ることを確認してください。テストは22件あります。1件でも落ちたら、
先に進まずに報告してください。

**2. デスクトップ常駐版の実装**

`HANDOFF.md` の第7章に、調査済みの前提と想定構成が書いてあります。
やりたいことは「どのアプリを使っていても、右 Alt を押すと録音が始まり、
もう一度押すと停止して、整形済みのテキストがカーソル位置に直接入る」ことです。

重要な制約:
- この PC に Rust と Visual Studio Build Tools は入っていないので Tauri は不可。Electron を使う
- Electron の globalShortcut では右 Alt 単独を登録できないので、Windows の
  低レベルキーボードフックが必要。PowerShell の Add-Type で C# を実行時
  コンパイルすれば追加インストール無しで実装できる
- 既存の `src/lib` （プロンプト・各社API・整形処理・辞書）はそのまま再利用できる
- 追加の費用が発生する構成にはしないこと

作業の進め方は `HANDOFF.md` の第8章に従ってください。
特に「編集したらコミット＆プッシュ」「Obsidian 側のバックアップも更新」
「実機確認できていないことは正直に伝える」の3点は必ず守ってください。

回答は日本語でお願いします。
